+++
date = '2026-08-10T16:10:49+08:00'
draft = false
title = 'K8s Var Lib Kubelet与etc Kubernetes区别'

+++

| 维度        | /etc/kubernetes                                             | /var/lib/kubelet                                            |
| ----------- | ----------------------------------------------------------- | ----------------------------------------------------------- |
| 目录类型    | 系统配置目录（静态）                                        | 服务运行数据目录（动态）                                    |
| 主要内容    | TLS 证书、kubeconfig、静态 Pod yaml                         | Pod 本地卷、运行状态、kubelet 运行配置、插件缓存            |
| 写入频率    | 极低（初始化 / 证书更新）                                   | 极高（Pod 启停、镜像拉取、日志写入）                        |
| Master 节点 | 完整（含 manifests、全部证书）                              | 和 Worker 结构一致，存放控制平面静态 Pod 的数据             |
| Worker 节点 | 仅 CA 根证书 + kubelet.conf                                 | 业务 Pod 全部本地存储                                       |
| 误删后果    | 节点无法认证集群，组件断连，可通过 kubeadm 重新生成证书修复 | 节点上所有 Pod 数据丢失，容器重建，业务中断                 |
| 运维场景    | 证书轮换、集群权限调整、静态 Pod 改造                       | 节点磁盘扩容、清理 Pod 残留、kubelet 参数调优、故障节点重置 |

| 对比项   | /etc                        | /var                                   |
| -------- | --------------------------- | -------------------------------------- |
| 数据属性 | 静态配置                    | 动态运行数据                           |
| 写入场景 | 手动修改配置、软件安装      | 服务运行、日志打印、数据写入、缓存生成 |
| 磁盘空间 | 很小，几十 MB 级别          | 极易扩容，几十 G~TB 级别               |
| 重启后   | 内容完全保留                | 大部分业务数据保留，临时 run 数据清空  |
| 故障影响 | 配置丢失→服务启动失败       | 数据丢失→业务数据损毁、日志丢失        |
| 清理操作 | 几乎不能删                  | 可清理日志、缓存释放空间               |
| K8s 对应 | /etc/kubernetes（证书配置） | /var/lib/kubelet（Pod 业务数据）       |

1. kubelet 启动读取 `/etc/kubernetes/kubelet.conf`（配置凭证，静态）
2. kubelet 运行时把 Pod 挂载、容器数据、日志全部写到 `/var/lib/kubelet`（动态可变）
3. master 节点 apiserver 等静态 Pod 配置在 `/etc/kubernetes/manifests`，容器落地存储在`/var/lib/kubelet/pods`
